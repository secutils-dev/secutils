// Browser stub -- grid visualisation pulls in canvas / svg-on-disk deps we
// don't need at runtime. Lifted from run-llama/liteparse's browser-compat
// stubs.
export async function renderGridVisualization(): Promise<void> {}
export async function renderAllVisualizations(): Promise<unknown[]> {
  return [];
}
